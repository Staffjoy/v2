package(default_visibility = ["@//visibility:public"])

load("@//third_party:go/build.bzl", "external_go_package")

external_go_package(
    base_pkg = "github.com/ttacon/libphonenumber",
    deps = [
        "@go_protobuf//:proto",
        "@go_builder//:builder",
    ]
)
