dist: bionic
sudo: required
language: go
env:
  - GO111MODULE=on
go:
  - 1.11
  - 1.12
go_import_path: v2.staffjoy.com
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - openjdk-11-jdk
      - bazel
      - wget
      - pkg-config
install:
  - make build
script:
  - make test
